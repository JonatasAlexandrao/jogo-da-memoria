const validateField = (function(){

  const module = {}
  module.validate = () => {

    //const $input = document.querySelector(`input-collabcode -${module._id}`)

   /* if($input.type == "email"){

    }
    else if ($input.type == "password"){

      if($input.lenght < 8) {
        
      }

    }*/
    console.log('menos de 8 digitos')

  }

  return {
    validate: module.validate
  }

})()

  